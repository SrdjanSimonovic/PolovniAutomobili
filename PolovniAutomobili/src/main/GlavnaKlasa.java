package main;

import java.util.List;
import java.util.Scanner;

import metode.MetodeJdbc;
import model.Automobil;
import validacija.Validacija;

public class GlavnaKlasa {

	public static void main(String[] args) {
		
		
		
		
	/*	Scanner scanner = new Scanner(System.in);
		System.out.println("Unesite svoj id: ");
		int id = Integer.parseInt(scanner.nextLine());
		System.out.println("Unesite svoju marku");
		int marka = Integer.parseInt(scanner.nextLine());
		
	
	    MetodeJdbc.azurirajPoMarki(marka, id);*/
		
		
		
	/*	List<Automobil> listaAutomobila = MetodeJdbc.dajSveAutomobile();
		
		System.out.println("id"+ " "+"cena"+" "+"marka"+" "+"godiste"+" "+"broj vrata"+" "+"pogon"+" "+"region"+ " "+"klima"+" "+"menjac");
		
		for (Automobil a : listaAutomobila) {
			System.out.println(a.getMarka()+" "+a.getCena()+" "+a.getGodiste()+" "+a.getBroj_vrata()+" "+a.getPogon()+" "+a.getRegion()+" "+a.getKlima()+" "+a.getMenjac());
		}*/
		
		/*
        Scanner scanner = new Scanner(System.in);
		
        System.out.println("Unesite marku: ");
	    int marka = Integer.parseInt(scanner.nextLine());
		
		System.out.println("Unesite cenu: ");
		double cena = Double.parseDouble(scanner.nextLine());
		
		System.out.println("Unesite godiste: ");
		int godiste = Integer.parseInt(scanner.nextLine());
		
		System.out.println("Unesite broj vrata: ");
		int broj_vrata = Integer.parseInt(scanner.nextLine());
		
		System.out.println("Unesite pogon: ");
		int pogon = Integer.parseInt(scanner.nextLine());
		
	    System.out.println("Unesite region: ");
	    int region = Integer.parseInt(scanner.nextLine());
	    
	    System.out.println("Unesite klima: ");
		int klima = Integer.parseInt(scanner.nextLine());
		
		System.out.println("Unesite menjac: ");
		int menjac = Integer.parseInt(scanner.nextLine());
			
		MetodeJdbc.ubaciUTabeluAutomobili(cena, marka, godiste, broj_vrata, pogon, region, klima, menjac);*/
	
	}

}
